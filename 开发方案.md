# 教师管理系统 — 开发方案

**版本**：V1.0  
**日期**：2026-05-29  
**基于**：《教师管理系统技术设计文档》V1.1

---

## 一、环境配置汇总

| 组件 | 地址 | 端口 | 认证信息 | 备注 |
|------|------|------|----------|------|
| MySQL | localhost | 3306 | root / 123456 | 数据库名: `teacher_system` |
| Redis | localhost | 6379 | 无密码 | |
| Milvus | localhost | 19530 | 无 token | |
| DeepSeek API (Pro) | `https://api.deepseek.com/anthropic` | — | `sk-e75757c21c3446509090f973a58713e7` | 复杂推理/NL2SQL |
| DeepSeek API (Flash) | `https://api.deepseek.com/anthropic` | — | 同上 Key | 快速响应/RAG |
| Embedding API | `https://dashscope.aliyuncs.com/compatible-mode/v1` | — | `sk-d716ce5d91f04563b2345e4ca19e64f2` | 阿里云 DashScope |

**自生成密钥**（写入 `.env`，请按需更换）：

| 密钥 | 值 | 用途 |
|------|-----|------|
| JWT_SECRET_KEY | `tms-jwt-8a7f3c2e1b4d9f06a5c3e8d7b2f1a409` | JWT 令牌签名 |
| JWT_REFRESH_SECRET_KEY | `tms-refresh-3f7a1d8c5b2e906f4a1c7e3d8b5f2067` | Refresh Token 签名 |
| AES_ENCRYPTION_KEY | `tms-aes-256-cbc-key-2026-05-29!!` (32字节) | 身份证/密保答案加密 |

---

## 二、项目目录结构

```
C:\teacher\teacher-management-system\
├── alembic/                          # 数据库迁移
│   ├── versions/
│   ├── env.py
│   └── alembic.ini
├── app/
│   ├── __init__.py
│   ├── api/
│   │   ├── __init__.py
│   │   ├── v1/
│   │   │   ├── __init__.py
│   │   │   ├── router.py             # 路由汇总
│   │   │   ├── deps.py               # 依赖注入（获取当前用户等）
│   │   │   └── endpoints/
│   │   │       ├── __init__.py
│   │   │       ├── auth.py           # 认证接口
│   │   │       ├── teachers.py       # 教师管理接口
│   │   │       ├── courses.py        # 课程管理接口
│   │   │       ├── affairs.py        # 事务管理接口
│   │   │       ├── attendance.py     # 考勤打卡接口
│   │   │       ├── notifications.py  # 消息通知接口
│   │   │       ├── files.py          # 文件上传接口
│   │   │       ├── reports.py        # 报表导出接口
│   │   │       ├── agent.py          # Agent SSE对话接口
│   │   │       └── logs.py           # 操作日志接口
│   ├── core/
│   │   ├── __init__.py
│   │   ├── config.py                 # Settings (pydantic-settings)
│   │   ├── security.py               # JWT、bcrypt、AES
│   │   ├── exceptions.py             # 自定义异常类
│   │   └── events.py                 # 全局异常处理器注册
│   ├── db/
│   │   ├── __init__.py
│   │   ├── session.py                # async SQLAlchemy engine & session
│   │   ├── models/
│   │   │   ├── __init__.py
│   │   │   ├── teacher.py            # teacher_info + user_auth
│   │   │   ├── course.py             # teacher_course
│   │   │   ├── affair.py             # teacher_affair
│   │   │   ├── attendance.py         # attendance_record
│   │   │   ├── notification.py       # system_notification
│   │   │   ├── log.py                # operation_log
│   │   │   └── riddle.py             # lantern_riddles
│   │   └── repositories/
│   │       ├── __init__.py
│   │       ├── base.py               # 通用 CRUD + 软删除支持
│   │       ├── teacher_repo.py
│   │       ├── course_repo.py
│   │       ├── affair_repo.py
│   │       ├── attendance_repo.py
│   │       ├── notification_repo.py
│   │       ├── log_repo.py
│   │       └── riddle_repo.py
│   ├── schemas/
│   │   ├── __init__.py
│   │   ├── common.py                 # 通用响应/分页模型
│   │   ├── auth.py
│   │   ├── teacher.py
│   │   ├── course.py
│   │   ├── affair.py
│   │   ├── attendance.py
│   │   ├── notification.py
│   │   ├── file.py
│   │   └── agent.py
│   ├── services/
│   │   ├── __init__.py
│   │   ├── auth_service.py
│   │   ├── teacher_service.py
│   │   ├── course_service.py
│   │   ├── affair_service.py
│   │   ├── attendance_service.py
│   │   ├── notification_service.py
│   │   ├── report_service.py
│   │   ├── file_service.py
│   │   └── agent/
│   │       ├── __init__.py
│   │       ├── agent_manager.py      # Agent 入口 & SSE 流式
│   │       ├── session_manager.py    # Redis 会话状态管理
│   │       ├── tool_manager.py       # LangChain 工具注册
│   │       ├── nl2sql_executor.py    # NL2SQL 执行器
│   │       ├── rag_retriever.py      # RAG 向量检索
│   │       ├── game_manager.py       # 二十四点 & 灯谜
│   │       └── prompt_templates.py   # 提示词模板
│   ├── middlewares/
│   │   ├── __init__.py
│   │   ├── auth_middleware.py        # JWT 认证中间件
│   │   ├── log_middleware.py         # 操作日志记录中间件
│   │   └── rate_limit_middleware.py  # 限流中间件
│   ├── tasks/
│   │   ├── __init__.py
│   │   ├── notification_tasks.py     # 课程提醒/生日祝福
│   │   └── agent_tasks.py            # Agent 相关异步任务
│   └── utils/
│       ├── __init__.py
│       ├── crypto.py                 # AES 加解密
│       ├── excel.py                  # Excel 导出工具
│       ├── redis_client.py           # Redis 连接管理
│       ├── milvus_client.py          # Milvus 连接管理
│       ├── teacher_id_generator.py   # teacher_id 生成器
│       ├── file_utils.py             # 文件安全校验
│       └── common.py                 # 通用工具函数
├── tests/
│   ├── __init__.py
│   ├── conftest.py
│   ├── test_auth.py
│   ├── test_teachers.py
│   ├── test_courses.py
│   ├── test_affairs.py
│   ├── test_attendance.py
│   ├── test_notifications.py
│   └── test_agent.py
├── frontend/                         # 前端项目（第四阶段）
│   └── README.md                     # 前端开发说明
├── uploads/
│   ├── avatars/                      # 头像存储目录
│   └── attachments/                  # 附件存储目录
├── .env                              # 环境变量（实际配置）
├── .env.example                      # 环境变量模板
├── .gitignore
├── requirements.txt
├── main.py                           # 应用入口
└── README.md
```

---

## 三、环境变量配置 (.env)

```ini
# ========== 应用配置 ==========
APP_NAME=TeacherManagementSystem
APP_ENV=development
APP_DEBUG=true
APP_HOST=0.0.0.0
APP_PORT=8000

# ========== MySQL ==========
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=123456
MYSQL_DATABASE=teacher_system

# ========== Redis ==========
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ========== Milvus ==========
MILVUS_HOST=localhost
MILVUS_PORT=19530
MILVUS_COLLECTION=teacher_knowledge

# ========== DeepSeek API ==========
DEEPSEEK_BASE_URL=https://api.deepseek.com/anthropic
DEEPSEEK_API_KEY=sk-e75757c21c3446509090f973a58713e7
DEEPSEEK_PRO_MODEL=deepseek-v4-pro
DEEPSEEK_FLASH_MODEL=deepseek-v4-flash

# ========== Embedding API (DashScope) ==========
EMBEDDING_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
EMBEDDING_API_KEY=sk-d716ce5d91f04563b2345e4ca19e64f2
EMBEDDING_MODEL=text-embedding-v3

# ========== JWT ==========
JWT_SECRET_KEY=tms-jwt-8a7f3c2e1b4d9f06a5c3e8d7b2f1a409
JWT_REFRESH_SECRET_KEY=tms-refresh-3f7a1d8c5b2e906f4a1c7e3d8b5f2067
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ========== AES 加密 ==========
AES_ENCRYPTION_KEY=tms-aes-256-cbc-key-2026-05-29!!

# ========== 文件上传 ==========
UPLOAD_DIR=./uploads
MAX_AVATAR_SIZE=2097152
MAX_ATTACHMENT_SIZE=10485760
ALLOWED_IMAGE_TYPES=jpg,jpeg,png,gif
ALLOWED_DOC_TYPES=pdf,doc,docx,xls,xlsx

# ========== 考勤规则 ==========
CHECK_IN_START=08:00
CHECK_IN_END=09:00
CHECK_OUT_START=17:00
CHECK_OUT_END=18:00

# ========== Agent ==========
AGENT_SESSION_TTL=86400
AGENT_MAX_HISTORY=20
```

---

## 四、依赖清单 (requirements.txt)

```
# Web框架
fastapi==0.110.0
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# 数据库
sqlalchemy[asyncio]==2.0.27
aiomysql==0.2.0
alembic==1.13.1
pymilvus==2.4.0

# 缓存/会话
redis==5.0.1
aioredis==2.0.1

# 认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# 加密
cryptography==42.0.5

# LLM / Agent
langchain==0.2.0
langchain-community==0.2.0
httpx==0.27.0
sse-starlette==2.0.0

# 数据处理
pydantic==2.6.1
pydantic-settings==2.1.0
openpyxl==3.1.2

# 文件处理
aiofiles==23.2.1
Pillow==10.2.0

# 工具
python-dateutil==2.8.2
loguru==0.7.2

# 测试
pytest==8.0.1
pytest-asyncio==0.23.5
httpx==0.27.0
```

---

## 五、开发阶段与任务拆分

### 第一阶段：基础架构与核心功能（预计 5-7 天）

| # | 任务 | 内容 | 产出文件 |
|---|------|------|----------|
| 1.1 | 项目初始化 | 创建目录结构、`.env`、`requirements.txt`、`.gitignore` | 全目录骨架 |
| 1.2 | 配置中心 | `core/config.py`，基于 pydantic-settings 读取所有环境变量 | `app/core/config.py` |
| 1.3 | 数据库连接 | `db/session.py`，异步 SQLAlchemy engine + session factory | `app/db/session.py` |
| 1.4 | Redis 连接 | `utils/redis_client.py`，异步 Redis 客户端 | `app/utils/redis_client.py` |
| 1.5 | Milvus 连接 | `utils/milvus_client.py`，连接与 collection 管理 | `app/utils/milvus_client.py` |
| 1.6 | 安全模块 | `core/security.py` — bcrypt 密码哈希、JWT 签发/验证、AES 加解密 | `app/core/security.py` |
| 1.7 | AES 加密工具 | `utils/crypto.py` — 身份证/密保 AES-256-CBC 加解密 | `app/utils/crypto.py` |
| 1.8 | 自定义异常 | `core/exceptions.py` — 7 个异常类 | `app/core/exceptions.py` |
| 1.9 | 全局异常处理 | `core/events.py` — 注册 FastAPI exception handlers | `app/core/events.py` |
| 1.10 | 通用 Schema | `schemas/common.py` — 统一响应模型、分页模型 | `app/schemas/common.py` |
| 1.11 | ORM 模型 | 全部 8 张表的 SQLAlchemy 模型（含 is_deleted 字段） | `app/db/models/*.py` |
| 1.12 | Alembic 初始化 | 配置 alembic env，生成初始迁移脚本 | `alembic/` 目录 |
| 1.13 | Base Repository | 通用 CRUD 基类，支持软删除过滤 `is_deleted=0` | `app/db/repositories/base.py` |
| 1.14 | teacher_id 生成器 | Redis INCR + MySQL 回退的 8 位 ID 生成 | `app/utils/teacher_id_generator.py` |
| 1.15 | 认证接口 | `/auth/login`, `/auth/refresh`, `/auth/logout`, `/auth/captcha`, `/auth/forgot-password`, `/auth/password` | `app/api/v1/endpoints/auth.py`, `app/services/auth_service.py` |
| 1.16 | JWT 中间件 | 解析 Bearer Token，注入 `current_user` 到 request.state | `app/middlewares/auth_middleware.py` |
| 1.17 | 教师 CRUD | `/teachers` 全套接口（列表、详情、新增、更新、软删除），含权限控制 | `app/api/v1/endpoints/teachers.py`, `app/services/teacher_service.py` |
| 1.18 | 文件上传 | `/files/upload` + `/teachers/{id}/avatar`，含类型/大小校验、UUID 重命名 | `app/api/v1/endpoints/files.py`, `app/services/file_service.py` |
| 1.19 | 路由汇总 | `router.py` 注册所有路由，`deps.py` 依赖注入 | `app/api/v1/router.py`, `app/api/v1/deps.py` |
| 1.20 | 应用入口 | `main.py` — FastAPI app 创建、中间件注册、事件注册、lifespan | `main.py` |
| 1.21 | 种子数据脚本 | 创建管理员账号（teacher_id: 32605001），初始化灯谜数据 | `scripts/seed.py` |

### 第二阶段：业务核心模块（预计 5-7 天）

| # | 任务 | 内容 | 产出文件 |
|---|------|------|----------|
| 2.1 | 课程管理 | CRUD + 冲突检测（同教师同时间段不可排课） | `courses.py` endpoint + service + repo |
| 2.2 | 事务管理 | 草稿创建 → 提交 → 审批（通过/驳回），状态流转控制 | `affairs.py` endpoint + service + repo |
| 2.3 | 考勤打卡 | 上班/下班打卡，自动判定状态（正常/迟到/早退/缺卡），日期唯一约束 | `attendance.py` endpoint + service + repo |
| 2.4 | 消息通知 | 通知列表、标记已读、未读计数、软删除 | `notifications.py` endpoint + service + repo |
| 2.5 | 操作日志中间件 | 自动记录每个请求的操作信息（operator_id, action, IP, result） | `app/middlewares/log_middleware.py` |
| 2.6 | 操作日志查询 | 管理员查询操作日志（分页、按操作人/时间筛选） | `app/api/v1/endpoints/logs.py` |
| 2.7 | 限流中间件 | 基于 Redis 的滑动窗口限流（60 秒内最多 100 次请求） | `app/middlewares/rate_limit_middleware.py` |
| 2.8 | 报表导出 | 教师名单 Excel 导出 + 考勤记录 Excel 导出 | `app/api/v1/endpoints/reports.py`, `app/services/report_service.py` |
| 2.9 | 课程提醒任务 | 定时检测明天有课教师，发送通知 | `app/tasks/notification_tasks.py` |
| 2.10 | 生日祝福任务 | 每日检测当天生日教师，发送祝福通知 | `app/tasks/notification_tasks.py` |

### 第三阶段：Agent 核心开发（预计 6-8 天）

| # | 任务 | 内容 | 产出文件 |
|---|------|------|----------|
| 3.1 | DeepSeek 客户端 | 封装 DeepSeek API 调用（chat completion + stream），区分 Pro/Flash 模型 | `app/utils/deepseek_client.py` |
| 3.2 | Embedding 客户端 | 封装 DashScope Embedding API | `app/utils/embedding_client.py` |
| 3.3 | 知识库入库 | 将规章制度、FAQ 文档向量化存入 Milvus | `scripts/load_knowledge.py` |
| 3.4 | RAG 检索器 | 向量检索 + 重排序，返回最相关 top-k 文档片段 | `app/services/agent/rag_retriever.py` |
| 3.5 | Schema Linking | 表结构向量化存入 Milvus，用户问题 → 检索相关表/字段 | `app/services/agent/nl2sql_executor.py`（内嵌） |
| 3.6 | NL2SQL 执行器 | Few-Shot 提示词 + Schema Linking → 生成 SQL → 参数化执行 → 结果格式化 | `app/services/agent/nl2sql_executor.py` |
| 3.7 | 游戏管理器 | 二十四点（随机出题 + 算式验证）+ 灯谜（随机出题 + 答案匹配） | `app/services/agent/game_manager.py` |
| 3.8 | 提示词模板 | 系统人设提示词、NL2SQL Few-Shot 模板、RAG 模板、游戏模板 | `app/services/agent/prompt_templates.py` |
| 3.9 | 会话管理器 | Redis 存储/恢复会话状态（user_id, current_game, game_state, last_intent, context） | `app/services/agent/session_manager.py` |
| 3.10 | LangChain 工具注册 | 将 execute_sql, rag_query, search, play_24_points, guess_riddle 注册为 LangChain Tool | `app/services/agent/tool_manager.py` |
| 3.11 | Agent 管理器 | 构建 AgentExecutor，意图识别 → 工具路由 → 结果润色 → SSE 流式输出 | `app/services/agent/agent_manager.py` |
| 3.12 | Agent API | `/agent/chat` (SSE)、`/agent/abilities`、`/agent/clear-session` | `app/api/v1/endpoints/agent.py` |
| 3.13 | 写操作确认 | INSERT/UPDATE 先返回预览，用户确认后执行（通过会话状态管理 pending_operation） | `agent_manager.py`（内嵌确认流程） |

### 第四阶段：前端开发（预计 8-12 天）

| # | 任务 | 内容 | 技术要点 |
|---|------|------|----------|
| 4.1 | 项目初始化 | Vue 3 + Vite + Element Plus + 古典风主题 | 自定义主题变量（深木色系、汉隶字体） |
| 4.2 | 布局框架 | 侧边导航 + 顶栏 + 内容区 + 面包屑 | Element Plus Layout 组件 |
| 4.3 | 登录/注册 | 登录表单、验证码、密码找回（密保问题验证） | 表单校验、图形验证码 |
| 4.4 | 教师管理页 | 列表（分页+筛选）、详情、新增/编辑表单、头像上传 | Table + Form + Upload |
| 4.5 | 课程管理页 | 课程列表、新增/编辑（含冲突提示）、按教师/学期筛选 | Gantt 或时间表视图 |
| 4.6 | 事务管理页 | 草稿列表、事务申请、审批操作（通过/驳回）、状态流转 | 流程跟踪 UI |
| 4.7 | 考勤打卡页 | 打卡按钮、月度日历视图、打卡记录、状态标记 | Calendar + 打卡动画 |
| 4.8 | 消息中心 | 通知列表、未读标记、消息详情、标记已读 | Badge + Drawer/Modal |
| 4.9 | Agent 对话页 | 聊天界面（气泡）、流式输出、语音输入按钮 | SSE EventSource |
| 4.10 | 报表导出 | 教师名单导出、考勤导出（触发下载） | Blob download |
| 4.11 | 操作日志页 | 日志列表（管理员可见）、按操作人/时间筛选 | Table + DatePicker |

### 第五阶段：测试（预计 3-5 天）

| # | 任务 | 内容 |
|---|------|------|
| 5.1 | 单元测试 | services 层核心逻辑覆盖（auth, teacher, course, attendance） |
| 5.2 | API 集成测试 | 所有端点 HTTP 测试（含权限校验、边界条件） |
| 5.3 | Agent 测试 | NL2SQL 生成准确性、RAG 检索相关性、游戏逻辑正确性 |
| 5.4 | 性能测试 | 关键接口压测（教师查询、打卡、Agent 对话），确保 200 QPS 以上 |

---

## 六、关键设计决策

### 6.1 DeepSeek API 调用方式
文档中 DeepSeek Base URL 为 `https://api.deepseek.com/anthropic`，这表明使用的是 Anthropic 兼容接口格式。调用时使用 Anthropic SDK 或 HTTP 请求格式（`messages` 数组），模型名设为 `deepseek-v4-pro` / `deepseek-v4-flash`。

### 6.2 Embedding 使用阿里云 DashScope
使用 `text-embedding-v3` 模型，通过 OpenAI 兼容接口调用。生成向量维度建议使用 1024。

### 6.3 LangChain 版本
使用 LangChain 0.2+，工具定义采用 `@tool` 装饰器，Agent 类型使用 `create_tool_calling_agent` + `AgentExecutor` 模式。

### 6.4 异步贯穿
从数据库会话、Redis、文件 I/O 到 HTTP 客户端，全链路使用 `async/await`，充分利用 FastAPI 异步性能。

---

## 七、工时汇总

| 阶段 | 内容 | 预计天数 |
|------|------|----------|
| 第一阶段 | 基础架构与核心功能 | 5-7 天 |
| 第二阶段 | 业务核心模块 | 5-7 天 |
| 第三阶段 | Agent 核心开发 | 6-8 天 |
| 第四阶段 | 前端开发 | 8-12 天 |
| 第五阶段 | 测试 | 3-5 天 |
| **合计** | | **27-39 天** |

---

## 八、风险与注意事项

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| DeepSeek API 格式兼容 | Agent 核心功能 | 提前验证 Anthropic 兼容接口的 chat/completion 和 streaming 端点 |
| Embedding 接口限频 | 知识库入库 | 批量入库时加入重试和间隔控制 |
| 课程冲突检测边界 | 排课准确率 | 明确定义冲突规则：同时段（schedule_info 相同）+ 同教师 |
| 软删除数据级联 | 数据一致性 | Repository 层统一处理，关联查询加 is_deleted=0 |
| NL2SQL 生成准确性 | Agent 体验 | Few-Shot 示例覆盖核心场景，输出 SQL 语法校验后再执行 |
| 本地 Milvus 稳定性 | RAG 检索 | 降级策略：Milvus 不可用时回退到关键词匹配 |

---

## 九、下一步行动

1. 确认本方案无异议后，先执行 **第一阶段 1.1-1.4**（配置中心 + 数据库/Redis/Milvus 连接）
2. 每完成一个阶段进行功能验收，再进入下一阶段
3. Agent 开发时先验证 API 连通性，确认模型可用
