# 教师管理系统 — 开发上下文文档

**生成日期**：2026-05-30  
**基于对话**：完整开发过程（从零搭建到第四阶段完成）

---

## 一、项目概述

蜀汉大数据与AI培训学校的教师管理系统。后端 Python/FastAPI，前端 Vue 3 + Element Plus（古典中国风主题），集成诸葛亮 Agent 智能助手（LangChain 工具调用、NL2SQL、RAG、游戏）。

### 技术栈

| 类别 | 技术 |
|------|------|
| 后端 | Python 3.11, FastAPI, SQLAlchemy 2.0 (async), Alembic |
| 数据库 | MySQL 8.0 (teacher_system), Milvus 3.0 (向量库), Redis 7.2 |
| 大模型 | DeepSeek API (deepseek-chat / deepseek-reasoner), Anthropic 兼容接口 |
| 向量化 | 阿里云 DashScope text-embedding-v3 (1024维) |
| 前端 | Vue 3 + Vite 8 + Element Plus + Pinia + Vue Router 4 |

### 目录结构

```
C:\teacher\teacher-management-system\
├── main.py                          # FastAPI 应用入口
├── .env / .env.example              # 环境变量
├── requirements.txt
├── app/
│   ├── api/v1/
│   │   ├── router.py                # 路由汇总 (10个路由模块)
│   │   ├── deps.py                  # 依赖注入
│   │   └── endpoints/               # 接口实现
│   │       ├── auth.py, teachers.py, files.py
│   │       ├── courses.py, affairs.py, attendance.py
│   │       ├── notifications.py, logs.py, reports.py, agent.py
│   ├── core/
│   │   ├── config.py                # pydantic-settings 配置中心
│   │   ├── security.py              # JWT + bcrypt + AES
│   │   ├── exceptions.py            # 7个自定义异常类
│   │   └── events.py                # 全局异常处理器
│   ├── db/
│   │   ├── session.py               # async SQLAlchemy
│   │   ├── models/                  # 9张表 ORM (teacher, course, affair, attendance, notification, log, riddle, sequence, user_auth)
│   │   └── repositories/            # 数据访问层 (base.py 含软删除支持)
│   ├── schemas/                     # Pydantic 模型 (auth, teacher, course, affair, attendance, notification, file, agent, common)
│   ├── services/
│   │   ├── auth_service.py, teacher_service.py, course_service.py
│   │   ├── affair_service.py, attendance_service.py, notification_service.py
│   │   ├── file_service.py, report_service.py
│   │   └── agent/                   # Agent 服务
│   │       ├── agent_manager.py     # 核心编排 + 模块路由
│   │       ├── session_manager.py   # Redis + 内存 fallback
│   │       ├── tool_manager.py      # 意图分类 + 工具路由
│   │       ├── nl2sql_executor.py   # Schema Linking + Few-Shot + SQL执行
│   │       ├── rag_retriever.py     # Milvus 向量检索
│   │       ├── game_manager.py      # 24点 + 猜灯谜
│   │       ├── deepseek_client.py   # DeepSeek API (Anthropic兼容)
│   │       └── prompt_templates.py  # 系统提示词 + Schema + Few-Shot
│   ├── middlewares/
│   │   ├── auth_middleware.py       # JWT Bearer Token 验证
│   │   ├── log_middleware.py        # 操作日志自动记录
│   │   └── rate_limit_middleware.py # 滑动窗口限流
│   ├── tasks/
│   │   └── notification_tasks.py    # 课程提醒 + 生日祝福
│   └── utils/
│       ├── redis_client.py, milvus_client.py
│       ├── crypto.py, teacher_id_generator.py
│       ├── file_utils.py, excel.py, common.py
├── frontend/                        # Vue 3 前端
│   ├── vite.config.js               # API 代理配置 (/api → localhost:8000)
│   ├── index.html
│   └── src/
│       ├── main.js                  # Pinia + Router + Element Plus 初始化
│       ├── App.vue, style.css       # 古典中国风主题 (深木色+金色+朱红)
│       ├── api/index.js             # Axios API 封装 + 拦截器
│       ├── router/index.js          # 路由 + 权限守卫
│       ├── stores/auth.js           # Pinia 认证状态
│       ├── layouts/MainLayout.vue   # 侧边栏导航 + 顶栏
│       └── views/
│           ├── Login.vue, Dashboard.vue
│           ├── teachers/TeacherList.vue
│           ├── courses/CourseList.vue
│           ├── affairs/AffairList.vue
│           ├── attendance/AttendancePage.vue
│           ├── notifications/NotificationList.vue
│           ├── agent/AgentChat.vue  # 诸葛亮对话 (模块按钮 + SSE解析)
│           ├── logs/LogList.vue
│           └── reports/ReportPage.vue
├── scripts/
│   ├── seed.py                      # 种子数据 (管理员 + 10条灯谜)
│   ├── load_knowledge.py            # 学校信息向量化 → Milvus
│   ├── test_all_v2.py               # 87项全面测试
│   └── test_phase1.py               # 第一阶段测试
├── alembic/                         # 数据库迁移
└── uploads/                         # 文件上传目录
```

---

## 二、环境配置

### .env 关键配置

```ini
# MySQL
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=123456
MYSQL_DATABASE=teacher_system

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Milvus
MILVUS_HOST=localhost
MILVUS_PORT=19530
MILVUS_COLLECTION=teacher_school_knowledge

# DeepSeek API (Anthropic 兼容接口)
DEEPSEEK_BASE_URL=https://api.deepseek.com/anthropic
DEEPSEEK_API_KEY=sk-e75757c21c3446509090f973a58713e7
DEEPSEEK_PRO_MODEL=deepseek-reasoner
DEEPSEEK_FLASH_MODEL=deepseek-chat

# Embedding API (DashScope)
EMBEDDING_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
EMBEDDING_API_KEY=sk-d716ce5d91f04563b2345e4ca19e64f2
EMBEDDING_MODEL=text-embedding-v3

# JWT
JWT_SECRET_KEY=tms-jwt-8a7f3c2e1b4d9f06a5c3e8d7b2f1a409
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440   # 24小时

# AES
AES_ENCRYPTION_KEY=tms-aes-256-cbc-key-2026-05-29!!
```

### 启动命令

```bash
# 后端
cd C:\teacher\teacher-management-system
PYTHONPATH=. python -m uvicorn main:app --host 0.0.0.0 --port 8000 --log-level warning

# 前端
cd C:\teacher\teacher-management-system\frontend
npx vite --host 0.0.0.0 ./frontend/

# 访问 http://localhost:5173
```

---

## 三、测试账号

| 角色 | 工号 | 密码 | 权限 |
|------|------|------|------|
| 管理员 | 32605001 | admin123 | 全部 (CRUD、审批、日志、删除) |
| 诸葛亮(教师) | 12605003 | test123456 | 受限 (只看自己数据) |
| 黄月英(教师) | 12605004 | test123456 | 受限 |
| 赵云(教师) | 12605002 | zhaoyun123 | 受限 |
| 张飞(领导) | 22605001 | zhangfei123 | 领导 (可审批、不可删用户、不可看日志) |

---

## 四、API 接口总览

| 模块 | 路径前缀 | 核心接口 |
|------|---------|---------|
| 认证 | `/auth` | login, refresh, logout, captcha, forgot-password, change-password |
| 教师 | `/teachers` | CRUD + 软删除 + 头像上传 + Excel导出 |
| 文件 | `/files` | 上传(图片/PDF/DOC/XLS)，安全校验 |
| 课程 | `/courses` | CRUD + 排课冲突检测(同教师同时段自动拦截) |
| 事务 | `/affairs` | 草稿→提交→审批(通过/驳回)，状态机保护 |
| 考勤 | `/attendance` | 打卡/签退，迟到自动判定，重复拦截 |
| 通知 | `/notifications` | 列表、标已读、未读计数 |
| 日志 | `/logs` | 管理员查询操作日志 |
| 报表 | `/reports` | 教师/考勤 Excel 导出 |
| Agent | `/agent` | SSE 流式对话、能力列表、灯谜下一题、清会话 |

### 统一响应格式

```json
{"code": 200, "data": {...}, "message": "success"}       // 成功
{"code": 401, "data": null, "message": "...", "error": ""} // 错误
```

**重要**：所有错误（含401/403/404/409/500）都返回 HTTP 200，业务 code 在 body 中。前端需检查 `body.code`，不能仅依赖 HTTP status。

---

## 五、Agent 功能详解

### 五大模块

| 模块 | key | 触发方式 | 说明 |
|------|-----|---------|------|
| 业务处理 | `business` | 点击按钮 + 输入 | NL2SQL 查询/修改（修改需确认） |
| 知识问答 | `knowledge` | 点击按钮 + 输入 | RAG 检索学校规章制度 |
| 闲聊 | `chat` | 点击按钮 + 输入 | 诸葛亮人设自由对话 |
| 二十四点 | `game_24` | 点击按钮自动出题 | LLM 解析自然语言→算式验证 |
| 猜灯谜 | `game_riddle` | 点击按钮自动出题 | 答对后显示"下一题"按钮 |

### 请求/响应格式

```json
// 请求
POST /api/v1/agent/chat
{"message": "你好", "module": "chat", "session_id": "可选"}

// 响应 (SSE)
data: {"type":"start","content":""}
data: {"type":"text","content":"主公..."}
data: {"type":"end","content":""}
data: [DONE]
```

### SSE 解析注意事项

**Windows 环境下 HTTP 响应使用 `\r\n` 换行**。解析前需 strip `\r`：
```javascript
const clean = line.replace(/\r$/, '');
```

### NL2SQL 数据表

测试数据已创建：5个教师 + 5门课程 + 2条事务。课程包括深度学习实战、NLP入门、Python基础、Hadoop基础、Spark实战。

### 知识库

学校信息已向量化存入 Milvus collection `teacher_school_knowledge`（8个文档块，1024维，IVF_FLAT 索引）。

---

## 六、已知问题与修复记录

| 问题 | 根因 | 修复 |
|------|------|------|
| `teacher_seq` 表缺少 `current_seq` 列 | 种子脚本未导入 TeacherSeq 模型 | 删除重建表，列名统一 |
| 创建教师时 `question1` 报错 | 密保问题字段未从 data 中移除 | 增加 `data.pop("questionN", None)` |
| Admin 被误删 (is_deleted=1) | 测试用 admin token 执行了 DELETE | 手动恢复 |
| passlib/bcrypt 兼容告警 | bcrypt 4.x 移除了 `__about__` | 不影响功能，忽略 |
| PyMilvus 2.x API 弃用 | 升级到 PyMilvus 3.0 | 用 `MilvusClient` 替代 `connections.connect` |
| Game 模块空消息被拒 | ChatRequest `min_length=1` | 改为 `default=""` |
| 模块切换不复用 session_id | 测试未传 sid | 前端正确传递 session_id |
| `sendMessage(msg)` 收到 MouseEvent | 模板 `@click="sendMessage()"` 传事件对象 | 区分自动触发和手动触发 |
| 游戏不自动生成 | `doSend('')` 空字符串被 `!text` 拦截 | 增加 `isAuto` 参数 |
| 图表名称不存在 | ChatDotRound/Odometer/Sunrise 可能无效 | 改用 ChatLineRound/Timer/Sunny |
| 前端 `useAuthStore is not defined` | 删除 Axios 导入时误删 import | 恢复 import |
| SSE 解析返回空 ("响应为空") | Windows `\r\n` 导致 JSON.parse 失败 | `.replace(/\r$/, '')` 清理行尾 |
| 401 "令牌无效或已过期" | JWT 60分钟过期 | 延长至 1440 分钟；前端加 body code 检测 |
| Redis 不可用导致登录失败 | get_redis() 抛异常 | auth_service 和 teacher_id_generator 加 try/except |
| 前端 Axios 拦截器可能干扰 fetch | agentAPI 使用 Axios，loadAbilities 调用可能触发 logout | AgentChat.vue 全部改用原生 fetch |

---

## 七、数据库表结构（关键字段）

所有核心表均有 `is_deleted TINYINT DEFAULT 0` 软删除字段。查询必须加 `is_deleted=0`。

- **teacher_info**: teacher_id(8位工号), name, department, title, status(1在职), is_deleted
- **user_auth**: teacher_id(PK), password_hash(bcrypt), question1/2/3_answer(AES加密)
- **teacher_course**: teacher_id, course_name, semester, schedule_info, location
- **teacher_affair**: teacher_id, affair_type(1-6), status(1草稿2审批中3通过4驳回5撤回)
- **attendance_record**: teacher_id, check_date, check_in_time, check_out_time, status(1正常2迟到3早退4缺卡)
- **system_notification**: receiver_id, title, content, type(1-4), is_read
- **operation_log**: operator_id, action, target_type, result
- **lantern_riddles**: riddle, answer, hint, source

### teacher_id 生成策略

格式：`[身份码][年2位][月2位][序号3位]` = 8位  
身份码：1=普通教师, 2=领导, 3=管理员  
Redis INCR + MySQL 回退

---

## 八、前端注意事项

1. **强制刷新**：代码修改后需 `Ctrl+Shift+R` 清除浏览器缓存
2. **Token 显示**：Agent 页面右上角显示当前 token 状态
3. **新对话按钮**：切换模块自动新会话；右上角"新对话"重置所有状态
4. **Vite 代理**：`/api` 和 `/uploads` 代理到 `localhost:8000`
5. **Element Plus 图标**：所有图标全局注册，使用 PascalCase 名称
6. **Pinia 持久化**：token 和 user 信息存储在 localStorage

---

## 九、测试

```bash
# 87项全面测试
python scripts/test_all_v2.py > /tmp/test_v2.txt 2>&1

# Agent 单独测试
python -c "..."  # 通过 Vite 代理测试所有5个模块
```

测试覆盖：认证(10项)、权限(4项)、教师CRUD(10项)、文件上传(4项)、课程冲突检测(5项)、事务审批流程(10项)、考勤打卡(5项)、通知(3项)、日志报表(5项)、角色权限(4项)、安全(6项)、异常(4项)、Agent全部5模块(8项)。

最后一次测试结果：**87/87 全部通过**。

---

## 十、待完成

- 第四阶段前端细节打磨（部分样式、响应式适配）
- 第五阶段单元测试 + 集成测试
- Redis 安装配置（当前使用内存 fallback）
- Docker Compose 部署配置
- 图形验证码实现
- 语音输入功能
